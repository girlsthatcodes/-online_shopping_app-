import 'package:flutter/material.dart';

class HomeScreen extends StatelessWidget {
  const HomeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text('Girls that code'),
          Text('Girls that code'),
          Text('Girls that code'),
          Text('Girls that code'),
          Text('Girls that code'),
          ],
      ),
    );
  }
}
